---
import { links } from '~/constants';
import Document from '~/layouts/Document.astro';
---

<Document>
  <div class="stars">
    <div class="twinkling">
      <main class="flex h-full items-center justify-center">
        <article
          class="card card-body h-1/2 w-3/4 animate-fadeIn !bg-surface-800 p-10 md:w-3/5 xl:w-1/2"
        >
          <div class="flex h-full w-full flex-col space-y-10 text-center">
            <h2>Hello, welcome to my little universe.</h2>
            <h4>Where would you like to go?</h4>
            <ul class="space-x-2.5 space-y-2.5">
              {
                links.map(({ href, icon: Icon, name }) => (
                  <a href={href} class="btn btn-filled-primary hover:btn-ghost-primary btn-lg">
                    <span>
                      <Icon />
                    </span>
                    <span>{name}</span>
                  </a>
                ))
              }
            </ul>
          </div>
        </article>
      </main>
    </div>
  </div>
</Document>
